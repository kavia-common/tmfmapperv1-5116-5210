# Flask TMF Translation Middleware - Example Environment Variables

# Upstream Django service base URL
DJANGO_BASE_URL=http://localhost:8000

# Preferred way to point to a native OpenAPI schema (takes precedence)
# Provide a full URL to the OpenAPI JSON (http/https)
NATIVE_OPENAPI_URL=

# Or provide a local filesystem path to the OpenAPI JSON
# Example: /app/schema/native_openapi_sample.json (inside container) or absolute on host if mounted
NATIVE_OPENAPI_PATH=

# Legacy var still supported (lower priority than NATIVE_OPENAPI_URL)
NATIVE_SCHEMA_URL=

# Port the Flask app listens on (do not change default to satisfy acceptance criteria)
SERVICE_PORT=3001

# Logging level
LOG_LEVEL=INFO
